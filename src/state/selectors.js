export const getStartField = (state) => state.startField;

export const getFinishField = (state) => state.finishField;

export const getSteps = (state) => state.steps;

export const getAnswer = (state) => state.answer;

export const getClickedField = (state) => state.clickedField;